# Makefile to compile the module in this directory

HEADERS = $(wildcard *.h)
SOURCES = $(wildcard *.cpp)
LIBRARIES = bmcustomcatalog
DYNAMIC_LINK_LIBRARIES = mort moutil momodest mofiber modata moio monet mohttp moconfig mologging mosql mosqlite3 movalidation mokernel bmnode bmcommon bmprotocol
DYNAMIC_MACOS_LIBRARIES = mocrypto modata mologging mofiber moevent movalidation moapp mocompress

# ----------- Standard Makefile
include @BITMUNK_DIR@/setup/Makefile.base
