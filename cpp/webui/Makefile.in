# Makefile to compile the module in this directory

HEADERS = $(wildcard *.h)
SOURCES = $(wildcard *.cpp)
LIBRARIES = bmwebui
DYNAMIC_LINK_LIBRARIES = mort moutil momodest moio mocrypto modata moconfig mocompress monet mohttp mologging movalidation mosql mosqlite3 mokernel bmnode bmcommon bmprotocol
DYNAMIC_WINDOWS_LIBRARIES = sqlite3
DYNAMIC_MACOS_LIBRARIES = sqlite3 mofiber moevent moapp

# ----------- Standard Makefile
include @BITMUNK_DIR@/setup/Makefile.base

