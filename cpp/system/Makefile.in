# Makefile to compile the module in this directory

HEADERS = $(wildcard *.h)
SOURCES = $(wildcard *.cpp)
LIBRARIES = bmsystem
DYNAMIC_LINK_LIBRARIES = mort moutil momodest moio moconfig monet mohttp modata mologging movalidation mokernel bmnode bmcommon bmprotocol
DYNAMIC_WINDOWS_LIBRARIES = mofiber
DYNAMIC_MACOS_LIBRARIES = moevent mofiber mocrypto moapp mocompress

# ----------- Standard Makefile
include @BITMUNK_DIR@/setup/Makefile.base
